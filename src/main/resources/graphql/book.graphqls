type Query{
    books: [Book]
    getBookById(id: ID): Book
}

type Mutation{
    createBook(name: String, pageCount: Int):Book
    createBookFull(book: BookIn):Book
}

input BookIn{
    name: String
    pageCount: Int
}

type Book {
    id: ID
    name: String
    pageCount: Int
}